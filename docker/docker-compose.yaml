version: '3.8'

services:
  zkbob-cloud:
    image: zkbob-cloud:latest
    container_name: zkbob-cloud
    volumes: 
      - ../params:/app/params/
      - ../configuration/base.yaml:/app/configuration/base.yaml
      - ../configuration/docker.yaml:/app/configuration/docker.yaml
    environment:
      APP_ENVIRONMENT: 'docker'
      RUST_LOG: "info"
      RUST_BACKTRACE: 1
    ports:
      - 8001:8001
    extra_hosts:
      - "host.docker.internal:host-gateway"